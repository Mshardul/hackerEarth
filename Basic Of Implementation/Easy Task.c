#include <stdio.h>

int main(int argc, char const *argv[])
{
	int n, i;
	long a, b;
	scanf("%d", &n);
	for(i=0; i<n; i++)
		scanf("%ld", &a[i]);
	for(i=0; i<n; i++)
		scanf("%ld", &b[i]);
	for(i=0; i<n; i++){
		if()
	}
	return 0;
}
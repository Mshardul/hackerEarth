#include <stdio.h>

int main(int argc, char const *argv[])
{
	int t, m, p, n;
	long arr[2000];
	scanf("%d", &t);
	for (int i = 0; i < t; ++i)
	{
		scanf("%d", &n);
		scanf("%d", &m);
		scanf("%d", &p);
		max_m = 
	}
	return 0;
}